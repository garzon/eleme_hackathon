language: py2
script:
  - sudo pip install -r requirements.txt
  #- gunicorn -w 5 -b "$APP_HOST:$APP_PORT" app:app
  - sudo apt-get install nginx ruby -y
  - erb /vagrant/nginx.conf > /vagrant/nginx.conf~
  - sudo rm /etc/nginx/sites-enabled/default
  - sudo rm /etc/nginx/sites-available/default
  - sudo cp /vagrant/nginx.conf~ /etc/nginx/sites-available/default
  - sudo ln -s /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
  - sudo service nginx stop
  - sudo service nginx start
  - sudo rm /tmp/uwsgi.sock -rf
  - uwsgi --processes 4 -s /tmp/uwsgi.sock --wsgi-file /vagrant/app.py --callable app --chmod-socket=666 -d /dev/null
  #- uwsgi -s /tmp/uwsgi.sock -w app:app
